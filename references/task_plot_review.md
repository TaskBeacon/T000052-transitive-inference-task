# Task Plot Review

- Flow: Diagram shows fixation, pair response, training-only feedback, and ITI; final test rows omit feedback.
- Conditions: Diagram covers training premise pairs, final test premise pairs, and final BD/AE probe trials.
- Timing: Diagram uses 400 ms fixation, 3000 ms response window, 800 ms training feedback, and 400 ms ITI.
- Keys: Diagram shows `Z` for left and `M` for right.
- Criterion: Diagram notes that training blocks repeat until 80% premise-pair criterion or repeat limit.
- README embed: `README.md` contains `![Task Flow](task_flow.png)` under `## 2. Task Flow`.
- Logo/header: Raw model output leaves blank header space; final `task_flow.png` is produced by `apply_taskbeacon_watermark.ps1`.
